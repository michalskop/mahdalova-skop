# KVIFF QA

Datované podsložky obsahují pouze záměrně uchované vizuální baselines.

Složka `2026-07-29` zachycuje desktopové a mobilní podoby landing page a
tehdejších tematických URL těsně před migrací pracovních podkladů. Aktuální
QA výstupy generuje `apps/web/scripts/qa-kviff.mjs` do ignorované složky
`test-results/kviff/`.
